function bool invert(matrix4 out, matrix4 in){
    out = in.invert();
    return true;
}

function double sqrt(double x){
    double top, bottom, middle;
    if(x < 1){
        top = 1.0;
        bottom = 0.0;
    } else {
        top = x;
        bottom = 1.0;
    }
    double eps = 0.001;
    middle = (top + bottom)/2;
    loop ( abs(middle * middle - x) > eps ){
        if(middle * middle > x){
            top = middle;
        } else {
            bottom = middle;
        }
        middle = (top + bottom)/2;
        print top, middle, bottom;
    }
    return middle;
}
